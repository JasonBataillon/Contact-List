import React, { useState, useEffect } from 'react';

function App({ selectedContactId, setSelectedContactId }) {
  return (
    <div>
      <h1>Selected Contact ID: {selectedContactId}</h1>
      {sadasda}
    </div>
  );
}
